"""Pydantic schemas for the CafeEvidencePack and MVP API responses.

The concrete models from agent_plan.md will land in the next implementation
step. This module exists now so backend modules can import from one stable
schema boundary.
"""
